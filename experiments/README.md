# Experiments

This directory stores experiment runbooks, prepared data, checkpoints, and reports.

The current sequence starts with Experiment 1:

- `exp01_original_latent_memory`: released reference plus retrained original latent-memory control.

Use `results/` for benchmark outputs and `experiments/*/reports/` for experiment notes that connect training checkpoints to evaluation results.
